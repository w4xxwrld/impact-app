import styled from 'styled-components/native';
import { ScrollView } from 'react-native';


const VerticalView = styled.ScrollView`
  height: auto;
  flex-direction: column;
  padding-bottom: 35px;
`;

export default VerticalView;
